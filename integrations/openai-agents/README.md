# Moved: Sprites for OpenAI Agents

The `sprites-openai-agents` package is now developed and released from
[superfly/sprites-openai-agents](https://github.com/superfly/sprites-openai-agents).

The PyPI package name and Python import path have not changed:

```bash
pip install sprites-openai-agents
```

```python
from sprites_openai_agents import SpritesSandboxClient
```

This directory remains as a migration pointer. The package's history through
version 0.1.0 remains available in the `sprites-py` Git history.
